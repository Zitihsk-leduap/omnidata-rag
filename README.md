A Python-based RAG project that combines PDF documents and news API data, performs vector similarity search with Chroma, and generates context-aware answers with Ollama LLM.
The two used models are:
    nomic-embed-text and smollm2.
